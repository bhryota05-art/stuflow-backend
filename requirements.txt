Flask==2.3.3
Flask-CORS==3.0.10
Flask-SQLAlchemy==3.0.5
Werkzeug==2.3.7  # For password hashing
python-dotenv==1.0.0
PyJWT==2.8.0
# Removed mysql-connector-python as we are now using in-memory SQLite (no driver needed)
